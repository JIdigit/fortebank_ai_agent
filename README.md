# ForteBank AI Business Analyst Agent

##  Описание

Интеллектуальный AI-агент для автоматизации работы бизнес-аналитика. Система помогает собирать требования, генерировать документацию и создавать аналитические артефакты.

##  Основные функции

### 1. **Диалоговый интерфейс**
- Ведение диалога с сотрудниками в формате чат-бота
- Уточнение деталей задачи и контекста
- Поддержка загрузки Excel файлов для анализа данных
- Сохранение истории диалогов через сессии

### 2. **Сбор и структурирование бизнес-требований**
- Автоматический анализ диалога
- Формирование структурированных требований
- Генерация полного документа требований включающего:
  - Цель проекта
  - Описание
  - Scope (границы проекта)
  - Бизнес-правила
  - KPI (ключевые показатели)
  - Заинтересованные стороны
  - Функциональные требования
  - Нефункциональные требования

### 3. **Генерация аналитических артефактов**
- **Use Cases** - детальные сценарии использования
- **User Stories** - пользовательские истории с критериями приемки
- **Диаграммы процессов** - в формате Mermaid

### 4. **Интеграция с Confluence**
- Автоматическое создание страниц в Confluence
- Публикация документации одним кликом
- Поддержка обновления существующих страниц

### 5. **Визуализация данных**
- Генерация графиков и диаграмм
- Использование корпоративных цветов ForteBank
- Автоматическое создание визуализаций на основе данных

##  Ключевые метрики успеха

### Performance
-  **Среднее время формирования бизнес-требования**: < 5 минут
-  **Время генерации артефактов**: < 2 минут

### Бизнес
-  **Снижение нагрузки на бизнес-аналитиков**: до 60%
-  **Автоматизация рутинных операций**: 80%

### Usability
-  **Доля успешных диалогов без помощи специалиста**: > 85%
-  **Интуитивный интерфейс** на русском языке

##  Установка и запуск

### Требования
- Python 3.8+
- Node.js 16+
- OpenAI API ключ

### Backend

1. Перейдите в директорию backend:
```bash
cd backend
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте `.env` файл:
```bash
OPENAI_API_KEY=your_openai_api_key_here

# Опционально: Confluence Integration
# CONFLUENCE_BASE_URL=https://your-domain.atlassian.net/wiki
# CONFLUENCE_USERNAME=your-email@example.com
# CONFLUENCE_API_TOKEN=your-api-token
# CONFLUENCE_SPACE_KEY=BA
```

5. Запустите сервер:
```bash
python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу: `http://localhost:8000`

### Frontend

1. Перейдите в директорию frontend:
```bash
cd frontend
```

2. Установите зависимости:
```bash
npm install
```

3. Запустите dev сервер:
```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173`

##  API Endpoints

### `/chat` (POST)
Основной endpoint для диалога с агентом.

**Параметры:**
- `message` (string) - сообщение пользователя
- `file` (file, optional) - Excel файл для анализа
- `session_id` (string, optional) - ID сессии для продолжения диалога

**Ответ:**
```json
{
  "response": "Ответ агента",
  "image": "base64_encoded_image",
  "session_id": "uuid"
}
```

### `/generate-requirements` (POST)
Генерация документа бизнес-требований.

**Тело запроса:**
```json
{
  "session_id": "uuid",
  "project_name": "Название проекта",
  "additional_context": {}
}
```

**Ответ:**
```json
{
  "success": true,
  "requirements": {...},
  "document": "Markdown документ"
}
```

### `/generate-artifacts` (POST)
Генерация аналитических артефактов.

**Тело запроса:**
```json
{
  "requirements": {...},
  "artifact_type": "use_cases" | "user_stories" | "process_diagram"
}
```

### `/publish-to-confluence` (POST)
Публикация документа в Confluence.

**Тело запроса:**
```json
{
  "project_name": "Название проекта",
  "document": "Markdown документ"
}
```

### `/health` (GET)
Проверка состояния сервиса.

##  Технологический стек

### Backend
- **FastAPI** - современный веб-фреймворк
- **OpenAI GPT-4o** - AI модель для анализа и генерации
- **Pandas** - обработка данных
- **Matplotlib** - визуализация данных
- **Requests** - интеграция с Confluence API

### Frontend
- **React** - UI библиотека
- **Vite** - сборщик и dev сервер
- **Lucide React** - иконки
- **React Markdown** - рендеринг markdown

## 🔧 Конфигурация Confluence

Для интеграции с Confluence:

1. Создайте API токен в Atlassian:
   - Перейдите в https://id.atlassian.com/manage-profile/security/api-tokens
   - Создайте новый токен

2. Добавьте параметры в `.env`:
```bash
CONFLUENCE_BASE_URL=https://your-domain.atlassian.net/wiki
CONFLUENCE_USERNAME=your-email@example.com
CONFLUENCE_API_TOKEN=your-api-token
CONFLUENCE_SPACE_KEY=BA
```

##  Примеры использования

### 1. Диагностика бизнес-ситуации
```
Пользователь: "У нас проблема с процессом одобрения кредитов. 
Время обработки заявки составляет 5 дней, клиенты жалуются."

Агент: "Понял вашу ситуацию. Давайте проанализируем процесс. 
Какие этапы включает текущий процесс одобрения?"
```

### 2. Сбор требований
```
Пользователь: "Нужно автоматизировать проверку документов"

Агент: "Отлично! Давайте уточним детали:
1. Какие типы документов нужно проверять?
2. Какие критерии проверки?
3. Кто будет использовать систему?"
```

### 3. Генерация документации
После диалога нажмите кнопку **"Создать документ"** для автоматической генерации:
- Полного документа бизнес-требований
- Use Cases
- User Stories
- Диаграмм процессов

##  Roadmap

-  Интеграция с Jira для создания задач
-  Поддержка диаграмм BPMN
-  Экспорт в Word/PDF
-  Мультиязычность (EN, KZ)
-  Голосовой ввод
-  Интеграция с корпоративными системами




